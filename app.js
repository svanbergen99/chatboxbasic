const chat=document.getElementById('chat');const form=document.getElementById('form');const input=document.getElementById('message');const saved=JSON.parse(localStorage.getItem('chatboxbasic_history')||'[]');for(const m of saved)add(m.role,m.text,false);function add(role,text,save=true){const el=document.createElement('div');el.className='msg '+(role==='me'?'me':'bot');el.textContent=text;chat.appendChild(el);chat.scrollTop=chat.scrollHeight;if(save){saved.push({role,text});localStorage.setItem('chatboxbasic_history',JSON.stringify(saved.slice(-200)))}}form.addEventListener('submit',async e=>{e.preventDefault();const message=input.value.trim();if(!message)return;input.value='';add('me',message);const wait=document.createElement('div');wait.className='msg bot';wait.textContent='...';chat.appendChild(wait);try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message})});const j=await r.json();wait.remove();add('bot',j.reply||j.error||'Geen antwoord.')}catch(err){wait.remove();add('bot','Fout: '+err.message)}});input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();form.requestSubmit()}});